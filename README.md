# prismic-node-proxy
A simple Express server which proxies API requests from the headless Prismic CMS, allowing the responses to be cached server-side, client-side, at the edge (e.g. CloudFlare/CloudFront/Akamai), or all of the above.
